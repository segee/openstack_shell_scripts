        echo 'To use this script, please first install the'
        echo 'python openstack client.'
        echo 'In ubuntu this can be done with:'
        echo 'sudo apt-get install python3-openstackclient'
        echo 
        echo 'Next, the openstack rc file must be downloaded'
        echo 'this can be done by logging into the openstack'
        echo 'dashboard, setting the appropriate project as'
        echo 'the active project, selecting access & security'
        echo 'API access, and download openstack RC file'
        echo 'copy the file to the directory containing this '
        echo 'script'
        echo
        echo 'Finally, create a text file named myi_password'
        echo 'that contains your openstack password.  Use'
        echo 'chmod 000 mypassword'
        echo 'so the file is not readable by anyone.'
        echo

